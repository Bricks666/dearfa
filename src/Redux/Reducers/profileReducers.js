const initialState = {
  backgroundPhoto: {
    url: "/Images/ProfileBackground/SunSet",
    alt: "Ваш фон...",
  },
};

export const profileReducers = (state = initialState, action) => {
  return state;
};
